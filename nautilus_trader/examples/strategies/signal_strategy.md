# signal_strategy.py 解读

## 定位

这是一个信号发布测试策略，重点不是交易，而是把内部计数或事件包装成 signal 对外发布。

## 触发数据

- `trade ticks`
- `quote ticks`

## 核心逻辑

1. 每收到一笔 tick 就更新内部计数。
2. 再通过 `publish_signal` 把信号发出去。
3. 常用于测试消息流、信号路由或外部组件对信号的消费能力。

## 停止行为

- 没有特别复杂的停止逻辑。

## 类型

- 订阅 / 诊断
